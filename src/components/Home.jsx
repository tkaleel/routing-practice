import React from 'react';

const Home = (props) => {
    return (
        <fieldset>
            <legend>Home.jsx</legend>
            <h3>Welcome home! 🏔🏔</h3>
        </fieldset>

    );
};

export default Home;
